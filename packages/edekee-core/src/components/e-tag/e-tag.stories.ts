export default {
  title: 'Components/Tag',
};

const Template = () => `<e-tag></e-tag>`;

export const closed = Template.bind({});
closed.args = {
  // first: 'Winnie',
  // middle: 'The',
  // last: 'Pooh'
};

export const opened = Template.bind({});
